{
  "name": "max-min-dist",
  "version": "1.0.1",
  "description": "generate approximate solutions to the max-min-dist dispersion problem",
  "main": "index.js",
  "scripts": {
    "lint": "eslint index.js",
    "lint-test": "eslint test/index.js",
    "test": "nyc --reporter=text mocha --timeout 120000",
    "coverage": "nyc --reporter=text-lcov mocha --timeout 120000 | coveralls",
    "go": "npm run lint && npm run lint-test && npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/drpaulbrewer/max-min-dist.git"
  },
  "keywords": [
    "dispersion",
    "approximation",
    "greedy-algorithm"
  ],
  "author": "drpaulbrewer@eaftc.com",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/drpaulbrewer/max-min-dist/issues"
  },
  "homepage": "https://github.com/drpaulbrewer/max-min-dist#readme",
  "devDependencies": {
    "coveralls": "^3.1.0",
    "eslint": "^7.1.0",
    "mocha": "^7.2.0",
    "nyc": "^15.0.1",
    "should": "^13.2.3"
  },
  "dependencies": {
    "@seregpie/bron-kerbosch": "^1.0.1",
    "prob.js": "^1.0.6"
  }
}
